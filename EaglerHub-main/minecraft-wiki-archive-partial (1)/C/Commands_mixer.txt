This article is a stub. 
You can help by expanding it. The talk page may contain suggestions.





This page describes content that has been removed and was only present in earlier versions of Bedrock Edition. 
Mixer was discontinued, and so was the command.

/mixer

Permission levelrequired

0

Restrictions

Cheat only

{
    "title": "<code>/mixer</code>",
    "rows": [
        {
            "field": "0",
            "label": "(link to Permission level article, displayed as Permission level<br>required)"
        },
        {
            "field": "(link to Cheat article, displayed as Cheat) only<br>",
            "label": "(link to Commands#Restrictions article, displayed as Restrictions)"
        }
    ],
    "invimages": [],
    "images": []
}

Controls Mixer interactivity.
Due to the shutdown of the Mixer service, this command was removed and permanently unusable, due to it requiring the Interactive lab setup inside the Mixer website.

Contents

1 Syntax
2 Arguments
3 Result
4 History
5 Navigation

Syntax
mixer <stop: string>
mixer <scene: string> <sceneName: string>
mixer <start: string> <version: int> [shareCode: string]
Arguments
start

Starts the interactive board.
version

Specifies the version ID of the player's board.
Example: 000001
shareCode

Specifies the share code of the player's board.
Example: 0000aaaa
scene

Changes scene.
sceneName

Specifies the name of the scene the player wants to change to.
Example: default
stop

Specifies that the player wants to stop the interactive board.
Result
start mode:

The MixPlay board begins.
scene mode:

The scene changes.
stop mode:

The MixPlay board shuts down.
Fails if the arguments are not specified correctly.

History
Bedrock Edition1.2.5beta 1.2.5.12
Added /mixer.1.16.210beta 1.16.210.53
Removed /mixer.1.16.210beta 1.16.210.54
Reimplemented /mixer.1.16.210beta 1.16.210.58
Removed /mixer again.
Navigation